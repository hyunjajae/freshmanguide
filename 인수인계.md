# 팀빌딩 접수 사이트 — 인수인계 문서

다음 회장·운영진에게 넘기는 실무 안내서입니다.
**코드를 몰라도 이 문서만 보면 행사를 치를 수 있게** 썼습니다.

읽는 순서
1. [이게 뭐 하는 사이트인가](#1-이게-뭐-하는-사이트인가)
2. [계정과 로그인](#2-계정과-로그인)
3. [행사 전 준비 — 순서대로](#3-행사-전-준비--순서대로)
4. [행사 당일 사용법](#4-행사-당일-사용법)
5. [행사 후 정리](#5-행사-후-정리)
6. [절대 하면 안 되는 것](#6-절대-하면-안-되는-것)
7. [서버·사이트 관리](#7-서버사이트-관리)
8. [내년에 다시 쓰기](#8-내년에-다시-쓰기)
9. [문제가 생겼을 때](#9-문제가-생겼을-때)
10. [넘겨받아야 할 것 목록](#10-넘겨받아야-할-것-목록)

---

## 1. 이게 뭐 하는 사이트인가

팀빌딩 행사 현장에서 **신입생이 왔는지 체크하는 사이트**입니다.

- **접수처**에서 신입생 이름을 검색해서 [접수] 버튼을 누릅니다.
- 그 순간 **각 LC 담당 FG의 휴대폰 화면에 0.2초 만에 반영**됩니다.
- FG는 "우리 조 몇 명 왔는지"를 실시간으로 봅니다.

예전에는 구글 스프레드시트로 했는데, FG 60명이 동시에 새로고침하면
구글 서버가 못 버텨서 행사 중에 멈추는 문제가 있어 새로 만들었습니다.

### 세 가지 역할

| 역할 | 누가 쓰나 | 할 수 있는 일 |
| --- | --- | --- |
| **운영자** | 회장 / 총괄 | 명단 올리기, 계정 만들기, 설정 변경, 결과 내보내기 |
| **접수처** | 접수 데스크 담당자 | 신입생 검색 → 접수 처리 / 취소, 현장 등록 |
| **진행 FG** | LC 담당 FG | 우리 조 명단·출석 현황 **보기만** (접수 못 함) |

> FG는 실수로라도 접수를 누를 수 없습니다. 화면에 버튼 자체가 없고,
> 설령 프로그램을 조작해도 서버에서 거부합니다.

---

## 2. 계정과 로그인

### 아이디 정리

| 역할 | 아이디 | 비밀번호 |
| --- | --- | --- |
| 운영자 | `freshmanguide` | **넘겨받은 비밀번호** (아래 참고) |
| 접수처 | `admin` | **넘겨받은 비밀번호** |
| 진행 FG | 각자 **성함** | 각자 **학번 10자리** |

> 비밀번호는 이 문서에 적지 않습니다. 이 폴더가 GitHub에 올라가기 때문입니다.
> 전임자에게 따로 받으세요. 못 받았다면 [9번](#9-문제가-생겼을-때)을 보세요.

### 로그인 방법

1. 사이트에 접속하면 **접수처 / 진행 FG** 두 개가 나옵니다.
2. 접수처를 고르고 아이디·비밀번호를 칩니다.
3. FG는 **성함과 학번**으로 들어갑니다. (별도 비밀번호 없음)

### 관리 화면 들어가기

명단을 올리거나 계정을 바꾸려면:

```
접수처로 로그인 → 오른쪽 위 톱니바퀴(⚙) → 운영자 아이디·비밀번호 입력
```

**접수처 계정만으로는 못 들어갑니다.** 접수 데스크 노트북을 잠깐 비웠을 때
누가 명단을 통째로 지우는 사고를 막기 위한 장치입니다.

### 비밀번호 바꾸는 법

관리 화면 → **계정** 탭

1. 위쪽 [운영자 / 접수처 / 진행 FG] 중에 고릅니다.
2. **바꾸려는 계정과 똑같은 아이디**를 칩니다.
3. 새 비밀번호를 넣고 [저장] → 덮어쓰기 됩니다.

### 남의 비밀번호·학번 확인하는 법

같은 화면 아래쪽 계정 목록에 **비밀번호와 학번이 그대로 보입니다.**
칩을 누르면 복사됩니다.

> FG가 "로그인이 안 돼요" 할 때 여기서 그 사람 학번을 확인해서 알려주면 됩니다.
> (등록된 학번이 오타면 여기서 바로 고칠 수 있습니다)

---

## 3. 행사 전 준비 — 순서대로

### ① 서버 살아있는지 확인 (행사 1주일 전)

무료 서비스라서 **오래 안 쓰면 서버가 자동으로 잠듭니다.** 잠들면 사이트가 아예 안 열립니다.

**자동 방지 장치가 걸려 있습니다.** GitHub이 3일마다 알아서 서버를 톡 건드려주기 때문에
평소에는 신경 안 쓰셔도 됩니다. ([자세히](#자동-깨우기-장치))

그래도 행사 1주일 전에 한 번은 확인하세요.

1. 사이트에 접속해서 로그인이 되는지 봅니다. **되면 끝.**
2. 안 열리면 [supabase.com](https://supabase.com) 로그인 (학생회 공용 구글 계정)
3. 프로젝트가 **Paused** 라고 되어 있으면 **Resume / Restore** 클릭
4. 2~3분 기다린 뒤 다시 접속

**행사 당일 아침에 이걸 발견하면 심장이 내려앉습니다. 반드시 미리 하세요.**

### ② 신입생 명단 올리기

구글폼으로 받은 응답을 그대로 씁니다.

1. 구글폼 응답 스프레드시트 열기
2. **파일 → 다운로드 → 쉼표로 구분된 값(.csv)**
3. 사이트 → 관리 화면 → **업로드** 탭
4. 받은 CSV 파일을 화면에 **끌어다 놓기**
   *(또는 스프레드시트에서 전체 선택 → Ctrl+C → 붙여넣기)*

**필요한 항목은 4개입니다.**

| 항목 | 필수 | 설명 |
| --- | --- | --- |
| 이름 | ✅ | |
| LC 번호 | ✅ | 1~93 (하루 31개 × 3일) |
| 연락처 | | 동명이인 구분 + 중복 검사에 씀 |
| 계열 | | |

> 신입생은 학번이 아직 없어서 학번은 받지 않습니다.

5. **열 이름은 자동으로 인식됩니다.** ("성함"→이름, "휴대폰 번호"→연락처 등)
   틀리면 직접 골라주면 됩니다.

6. 자동으로 검사 결과가 나옵니다.

| 표시 | 뜻 | 어떻게 되나 |
| --- | --- | --- |
| **정상** | 문제 없음 | 그대로 올라감 |
| **자동 제거** | 이름·연락처·LC·계열이 **전부** 똑같은 완전 중복 | 묻지 않고 하나만 남김 |
| **확인 필요** | 연락처가 겹치거나 / 형식이 이상하거나 / LC 범위 밖 | **직접 보고 판단**, 체크 풀면 제외 |
| **빨간 줄** | 이름이나 LC가 없음 | 올릴 수 없어서 자동 제외 |

7. **전체 교체**를 고르고 `전체교체` 라고 직접 입력한 뒤 업로드

> 예전에 스프레드시트에 "검토 필요" 탭 만들어서 하던 중복 검사가 이 화면 하나로 합쳐졌습니다.

### ②-b 올린 명단 확인하기

관리 화면 → **명단** 탭에서 올라간 명단을 전부 볼 수 있습니다.

- 위쪽 검색창에 **이름 / 초성 / LC 번호 / 연락처**를 치면 걸러집니다.
  (숫자 1~2자리는 LC 번호로, 3자리 이상은 연락처로 찾습니다)
- **일차**와 **접수 완료 / 미접수**로도 거를 수 있습니다.
- 잘못 올라간 사람은 오른쪽 **[삭제]** 로 지웁니다. 확인창이 한 번 더 뜹니다.

> 한 번에 100명씩 보여주고 아래 **[더 보기]** 로 늘립니다. (1,600명을 한 번에 그리면 폰이 버벅입니다)
> 삭제는 **되돌릴 수 없습니다.** 실수로 지웠다면 접수 화면의 **현장 등록**으로 다시 넣으세요.

### ③ FG 계정 만들기

관리 화면 → **계정** 탭 → **FG 일괄 등록**

엑셀에 이렇게 정리해서 통째로 복사 → 붙여넣기:

```
성함     학번          담당LC
홍길동   2024000001    1,2
김철수   2024000002    3
이영희   2024000003    4,5,6
```

- 담당 LC는 한 칸에 `1,2,3` 이라고 써도 되고 칸을 나눠 써도 됩니다.
- 올린 뒤 목록에서 **빨간색 "담당 LC 없음"** 이 있는지 꼭 확인하세요.
  그 FG는 로그인해도 빈 화면만 봅니다.

### ④ 설정 확인

관리 화면 → **설정** 탭

- **행사 이름** — 첫 화면에 뜨는 제목
- **하루 LC 개수** / **총 일수** — 기본 31개 × 3일 = LC 93개
  내년에 규모가 바뀌면 **여기서만 고치면 됩니다. 코드 안 건드려도 됩니다.**

### ⑤ 리허설 (행사 전날)

- 접수처 노트북으로 로그인 → 아무나 검색 → 접수 → **취소**까지 해보기
- FG 한 명 폰으로 로그인해서 그 변화가 바로 보이는지 확인
- 행사장 와이파이가 약하면 **LTE/5G로 하는 게 낫습니다**

---

## 4. 행사 당일 사용법

### 접수처

**검색 → 접수** 이게 전부입니다.

- 이름을 칩니다. **일부만 쳐도** 되고, **초성(ㄱㅎㄱ)** 으로도 찾아집니다.
- 동명이인이면 **연락처와 LC**가 같이 보이니 그걸로 구분하세요.
- 오른쪽 [접수] 버튼을 누르면 끝. 초록색으로 바뀝니다.
- 숫자만 3자 이상 치면 **연락처 뒷자리 검색**도 됩니다.

**빠르게 하는 요령**

- 검색 결과에 아직 접수 안 한 사람이 **딱 1명**이면 **Enter만 눌러도 접수**됩니다.
- LC 번호를 알면 옆 칸에 넣어서 범위를 좁히세요.

**잘못 눌렀을 때**

- 초록색으로 바뀐 사람 오른쪽에 [취소] 버튼이 있습니다. 확인창이 한 번 더 뜹니다.

**명단에 없는 사람이 왔을 때**

- 아래쪽 **[현장 등록]** → 이름·LC 넣고 저장하면 바로 추가 + 접수됩니다.
- 이렇게 넣은 사람은 명단에 **'현장'** 표시가 붙어서 나중에 구분됩니다.

**두 창구를 동시에 운영해도 됩니다.** 같은 사람을 동시에 눌러도
한 번만 처리되고, 나중에 누른 쪽에는 "이미 접수되었습니다"라고 뜹니다.

### 진행 FG

- 로그인 → **1/2/3일차 선택** → 우리 조 명단
- **미접수**가 위에, **접수 완료**가 아래에 나옵니다.
- 접수처에서 누르면 **새로고침 없이 바로** 바뀝니다.
- 명단 보는 동안 폰 화면이 자동으로 안 꺼집니다.
- 담당 LC 신입생의 **연락처를 전부 볼 수 있습니다.** (안 온 애한테 전화할 때)
- 왼쪽 위 **[‹ 날짜]** 로 다른 날짜를 볼 수 있습니다.

### 마감 직전에 유용한 것

접수처 아래 **[미접수자]** 버튼

- 아직 안 온 사람 목록이 LC별로 나옵니다.
- **[복사]** 를 누르면 `[LC01] 김철수, 이영희` 형태로 복사돼서
  단톡방에 바로 붙여넣을 수 있습니다.
- **[CSV]** 로 파일로도 받을 수 있습니다.

**[통계]** 버튼은 전체·일차별·LC별 접수율을 보여줍니다. 15초마다 자동 갱신됩니다.

---

## 5. 행사 후 정리

### 결과 저장하기 (꼭 하세요)

관리 화면 → **업로드** 탭 → 맨 위 **명단 내보내기**

- 전체 / 접수 완료만 / 미접수만, 일차별로 골라서 받을 수 있습니다.
- **이름, LC, 일차, 계열, 연락처, 접수여부(O/X), 접수시각, 현장등록여부**가 저장됩니다.
- 엑셀에서 바로 열립니다.

**내년 명단을 올리면 올해 기록은 사라집니다. 반드시 먼저 받아두세요.**

### 개인정보 정리

행사가 끝나고 결과 파일을 받아뒀다면, 학생 개인정보를 서버에 계속 둘 이유가 없습니다.
[8번](#8-내년에-다시-쓰기)처럼 내년에 프로젝트를 새로 만들면 자연스럽게 정리됩니다.

---

## 6. 절대 하면 안 되는 것

| 하지 말 것 | 왜 |
| --- | --- |
| **행사 당일에 "전체 교체" 업로드** | 그때까지 찍은 접수 기록이 전부 사라집니다. 당일 인원 추가는 **현장 등록**을 쓰세요 |
| **명단 탭에서 함부로 삭제** | 되돌릴 수 없습니다. 접수 완료된 사람을 지우면 기록도 같이 사라집니다 |
| **운영자 비밀번호를 카톡·문서에 적기** | 이 계정 하나로 전체 명단을 지울 수 있습니다 |
| **Supabase에서 `service_role` 키 복사해서 코드에 넣기** | 이건 관리자 열쇠입니다. `anon` 키만 씁니다 |
| **접수처 계정을 FG들에게 알려주기** | FG가 접수를 눌러버리면 현장이 꼬입니다 |
| **행사 직전에 LC 개수·일수 설정 바꾸기** | 이미 올린 명단의 일차 구분이 통째로 달라집니다 |
| **비밀번호를 코드 파일에 적기** | GitHub에 그대로 공개됩니다 |

---

## 7. 서버·사이트 관리

### 구성 요약

```
[신입생·FG·접수처의 브라우저]
            ↓
   Vercel (사이트 화면을 보여줌)
            ↓
  Supabase (명단과 접수 기록을 저장)
```

- **Supabase** = 데이터베이스. 예전의 구글 스프레드시트 자리.
- **Vercel** = 사이트를 인터넷에 띄워주는 곳.
- **GitHub** = 코드 보관소. 여기에 올리면 Vercel이 자동으로 사이트를 갱신합니다.

셋 다 **무료 요금제**로 충분합니다. 결제 정보 넣을 일 없습니다.

### Supabase에서 할 수 있는 일

[supabase.com/dashboard](https://supabase.com/dashboard) 로그인 후:

| 하고 싶은 것 | 어디서 |
| --- | --- |
| 서버 깨우기 | 프로젝트 목록에서 **Resume** |
| 데이터 직접 보기 | 왼쪽 **Table Editor** → `participants` |
| 데이터 백업 | Table Editor → 우측 상단 **Export** |
| 비밀번호 강제 변경 | 왼쪽 **SQL Editor** ([아래 참고](#비밀번호를-잊어버렸을-때)) |

### 자동 깨우기 장치

`.github/workflows/keep-alive.yml` 이라는 파일이 GitHub에 들어 있습니다.
**3일마다 자동으로 서버를 한 번 건드려서 잠들지 않게 합니다.** 사람이 할 일은 없습니다.

**처음 한 번만 설정이 필요합니다.** (이미 되어 있다면 넘어가세요)

1. GitHub 저장소 → **Settings** → 왼쪽 **Secrets and variables → Actions**
2. **New repository secret** 을 눌러 두 개를 등록합니다.

   | 이름 | 값 |
   | --- | --- |
   | `SUPABASE_URL` | Supabase → Project Settings → API 의 **Project URL** |
   | `SUPABASE_ANON_KEY` | 같은 화면의 **anon public** 키 |

3. **Actions** 탭 → 왼쪽 **Supabase 깨워두기** → **Run workflow** 로 한 번 눌러서 초록불이 뜨는지 확인

> 잘 돌고 있는지 확인하려면 **Actions** 탭에 3일마다 초록색 체크가 쌓이는지 보면 됩니다.
> 빨간 X 가 뜨면 서버가 이미 잠긴 것이니 Supabase에서 Resume 하세요.

> ⚠️ GitHub은 저장소에 60일간 아무 변화가 없으면 이런 자동 작업을 꺼버립니다.
> 그래서 이 작업이 돌 때마다 `.github/last-ping.txt` 에 날짜를 적어 커밋합니다.
> 그래도 혹시 꺼졌다면 Actions 탭에 "Enable workflow" 버튼이 뜨니 눌러주면 됩니다.

### Vercel에서 할 수 있는 일

[vercel.com](https://vercel.com) 로그인 후:

| 하고 싶은 것 | 어디서 |
| --- | --- |
| 사이트 주소 확인 | 프로젝트 → **Domains** |
| 다시 배포 | **Deployments** → 맨 위 항목 → **Redeploy** |
| 접속 주소 바꾸기 | **Settings → Domains** |
| Supabase 주소 교체 | **Settings → Environment Variables** |

> 코드를 GitHub에 올리면 Vercel이 **1~2분 안에 자동으로** 사이트를 갱신합니다.
> 따로 뭘 누를 필요 없습니다.

### 사이트 화면을 고치고 싶을 때

코드를 만질 줄 안다면 `README.md`에 개발자용 설명이 있습니다.
색깔만 바꾸고 싶다면 `src/styles.css` 맨 위 색 목록만 고치면 사이트 전체가 바뀝니다.

코드를 모른다면 **건드리지 마세요.** 대신 관리 화면의 **설정** 탭에서
행사 이름과 LC 개수는 바꿀 수 있습니다.

---

## 8. 내년에 다시 쓰기

무료 서버는 안 쓰면 잠기고, 오래 두면 지워질 수도 있습니다.
그래서 **매년 새로 만드는 것을 권장합니다. 10분이면 됩니다.**

### 순서

1. **작년 결과 백업** — 관리 화면에서 명단 내보내기 (안 했다면 지금)
2. Supabase에서 **New project** 생성
   - 반드시 **학생회 공용 구글 계정**으로
   - Region은 `Northeast Asia (Seoul)`
3. 왼쪽 **SQL Editor** → `supabase/schema.sql` 파일 내용 전체 복사 → 붙여넣기 → **Run**
4. 같은 SQL Editor에서 비밀번호 설정 (아래 두 줄, 따옴표 안만 바꾸세요)

   ```sql
   update accounts set login_key = '새운영자비밀번호'
    where role = 'MANAGER' and login_id = 'freshmanguide';

   update accounts set login_key = '새접수처비밀번호'
    where role = 'ADMIN' and login_id = 'admin';
   ```

5. Supabase **Project Settings → API** 에서 두 값 복사
   - `Project URL`
   - `anon` `public` 키
6. Vercel → **Settings → Environment Variables** 에서 두 값 교체 후 **Redeploy**
7. 새 명단 업로드 + 새 FG 계정 등록

---

## 9. 문제가 생겼을 때

### 사이트가 아예 안 열림

거의 100% **서버가 잠든 것**입니다.
Supabase 대시보드 → **Resume** → 2~3분 대기.

### 로그인이 안 됨

1. 앞뒤에 **띄어쓰기**가 들어갔는지 확인 (복사해서 붙여넣을 때 자주 생깁니다)
2. FG라면 관리 화면 → 계정 탭에서 **그 사람 학번이 제대로 등록됐는지** 확인
3. 등록된 학번이 틀렸으면 그 자리에서 고치면 됩니다

### 비밀번호를 잊어버렸을 때

Supabase → **SQL Editor** → New query → 붙여넣고 Run:

```sql
update accounts set login_key = '새비밀번호'
 where role = 'MANAGER' and login_id = 'freshmanguide';
```

접수처라면 `'MANAGER'` 대신 `'ADMIN'`, `'freshmanguide'` 대신 `'admin'`.

### FG 화면이 텅 비어 있음

담당 LC가 등록 안 된 것입니다.
관리 화면 → 계정 탭에서 그 FG 옆에 **빨간 "담당 LC 없음"** 이 있는지 확인하고 넣어주세요.

### 접수했는데 FG 화면에 안 뜸

- 최대 60초 안에는 저절로 맞춰집니다.
- 급하면 FG 화면 오른쪽 위 **새로고침 아이콘**을 누르세요.
- 여러 명이 계속 그러면 **와이파이 문제**일 가능성이 큽니다.

### 명단이 사라졌다

**"전체 교체"로 업로드**했을 가능성이 큽니다.
Supabase → Table Editor → `participants` 에서 실제로 비었는지 확인하고,
백업해둔 CSV가 있으면 다시 올리세요. **접수 기록은 복구되지 않습니다.**

### 로그인은 되는데 명단이 안 뜨고 버튼이 다 안 먹음

데이터베이스 함수가 꼬인 것입니다. **`supabase/schema.sql` 전체를 SQL Editor에 다시 붙여넣고 Run** 하세요.
맨 아래에 **"설치 완료"** 라고 나오면 정상입니다. 그 표가 안 보이면 중간에 멈춘 것이니
빨간 오류 메시지를 확인하세요. (파일이 길어서 일부만 붙여넣어지는 경우가 많습니다)

실행 후에는 사이트에서 **Ctrl+Shift+R**(맥은 Cmd+Shift+R)로 강제 새로고침 하세요.

### 화면이 하얗게만 나옴

브라우저에서 **F12 → Console** 탭의 빨간 글씨를 캡처해서 개발 가능한 사람에게 보여주세요.

---

## 10. 넘겨받아야 할 것 목록

전임자에게 이것들을 받아야 사이트를 이어받을 수 있습니다.
하나라도 빠지면 나중에 곤란해집니다.

- [ ] **학생회 공용 구글 계정** 아이디·비밀번호 (Supabase 로그인용)
- [ ] **Supabase** 계정 접근 권한
- [ ] **Vercel** 계정 접근 권한
- [ ] **GitHub** 계정 접근 권한 (코드 보관소)
- [ ] **운영자 비밀번호** (`freshmanguide`)
- [ ] **접수처 비밀번호** (`admin`)
- [ ] **사이트 주소** (FG들에게 공유할 링크)
- [ ] **작년 결과 CSV** (참고용)

> 계정이 전임자 **개인** 구글 계정으로 되어 있으면 반드시 공용 계정으로 옮기세요.
> 졸업하고 연락 끊기면 사이트를 통째로 못 씁니다.

---

**만든 사람**: 명륜 18기 회장 김현규
**문서 최종 수정**: 2026년 8월
